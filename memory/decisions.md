# 技術決策紀錄

## 2026-07-27 — 題庫「是否發布」預設改為自動發布（不再事前審核）【已凍結】

**決策：** A1-出題 workflow（n8n `IpxBWR3Nbg2z798v`，「寫入 Notion 題庫」節點）的 `是否發布` checkbox，建立頁面時預設打勾（`checkboxValue: true`）。AI 生成的題目不再等待人工審核才上架，而是直接發布到官網，事後才由使用者自行抽查、有問題的題目再手動取消勾選下架。

**理由：** 使用者明確要求「這部份要請他預設全是發布，有問題我會自行勾掉」——把審核模式從「發布前擋一道」改成「發布後抽查」，減少每月手動勾一百多題的負擔，風險使用者自行承擔。

**影響範圍：**
- 只影響*之後*新建立的題目；2026-08 那批 171 題（在此決策生效前用舊行為建立）仍是未勾狀態，維持人工審核（含挑掉看圖題）後手動發布，此決策不回溯套用
- checkbox 預設值本身不做內容把關；已知的看圖題等問題仍需靠 A1 prompt 端的禁圖規則過濾（已於同次對話強化）
- 已凍結：不再重新討論是否恢復「預設不發布、需人工審核」的舊行為，除非使用者主動重啟這個議題

**相關：** 同次對話另修正 A1 出題 JSON 轉義 bug（8月題目 0 生成）、A2 時區 bug（週六發布查詢日期永遠對不上）；詳見 `PROJECT_STATE.md` 2026-07-27 條目。

---

## 2026-07-12 — 記錄：「乾淨網址」/news/:slug 的轉址規則不在這個 repo 裡

**背景：** 調查桌面 PWA 打開最新消息內頁解崩的 bug（見 `memory/bugs.md` 同日條目）時發現，`/news/:slug` 這種乾淨網址能正確顯示 `news-single.html` 的內容，靠的是一條 **Cloudflare 儀表板端的 Transform/Redirect Rule**（commit `69af49a` message 提到「搭配 Cloudflare rewrite」），這條規則本身完全沒有寫進這個 repo 的任何檔案，只存在 Cloudflare 帳號設定裡。

**決策：** 暫不把規則內容抄寫進 repo（沒有實際存取 Cloudflare 儀表板核對確切規則內容，不確定就不亂寫，避免文件跟現況兜不起來）。先記錄「這個依賴存在」這件事，讓下一次有人查這一區塊的 bug 時，知道要同時去 Cloudflare 儀表板核對，而不是只看程式碼就以為看到全貌。

**待辦（manko 有空進 Cloudflare 儀表板時）：** 把 `/news/:slug` 對應的實際 Transform Rule（比對條件、rewrite 目標）截圖或抄錄下來，補進這份文件，讓這條規則不再是「只有 Cloudflare 帳號知道」的隱性依賴。

**Why：** `news-single.html` 是這個 repo 目前變動最頻繁的檔案（一個月內改了 5 次以上，含一次 revert），任何依賴 dashboard 設定卻沒有記錄下來的部分，都會讓未來的除錯變成憑空猜測，跟這次調查耗掉的時間一樣。

---

## 2026-07-07 — 出版社設定更新為 115 學年度（旭光國小）

**決策：** 直接修改 n8n workflow `AI-自動出題（每月15日）`（ID `IpxBWR3Nbg2z798v`）「建立出題任務清單」code 節點裡寫死的出版社對照物件，改成 115 學年度版本，不走 Admin Panel 上傳 AI 分析那條路。

**背景：**
- 使用者提供旭光國小 115 學年度教科書選用表截圖，8/1 起新學年生效
- 追查發現規格文件（`Mina_題庫系統_封板規格_V2.0.md` 第12節）描述的「上傳教科書照片 → AI Vision 分析 → 寫入 Notion『出題規範』DB → 出題時讀取」這套機制**沒有真的接上**：Admin Panel 上傳後確實會透過 n8n workflow `j5e127XhuYpXh01N` 寫入出題規範 DB（`455a66be-6c6b-4b59-a5f6-c5342d70696f`），但實際負責出題的 workflow `IpxBWR3Nbg2z798v` 完全沒有節點讀取這個 DB——出版社對照表是寫死在「建立出題任務清單」code 節點的 JS 物件裡，AI prompt 只拿到 `{{ $json.publisher }}` 這個字串，單元名稱靠 AI 自己用「教材版本＋週次」猜
- 時間壓力：這個 workflow 每月15日 cron 出「下個月」的題，7/15 那次就是出 8 月份（115學年度第一批），必須在那之前改完

**新舊對照（節點內物件，2026-07-07 更新）：**
| 年級 | 數學：114→115 | 英文：114→115 |
|---|---|---|
| 小一 | 康軒→翰林 | 康軒WW（不變，學校無開課，沿用補習班既有版本） |
| 小二 | 翰林→康軒 | 康軒WW（不變，同上） |
| 小三 | 康軒（不變） | 康軒WW→何嘉仁SuperFun |
| 小四 | 南一→康軒 | 康軒WW（不變） |
| 小五 | 康軒（不變） | 何嘉仁SuperFun→康軒WW |
| 小六 | 南一→康軒 | 康軒WW→何嘉仁SuperFun |

**Why：** 求快，7/15 前一定要生效；Admin Panel 那條 AI Vision 路線目前是斷頭路，串接需要額外開發（讀取出題規範DB、改 prompt 組裝邏輯），範圍較大，這次先不做。

**風險/後續：**
- 明年（116學年度，2027/8 起）一樣要手動來改這個 code 節點，除非之後決定投資把 Admin Panel 出版社上傳跟這個 workflow 真的串起來
- `Mina_題庫系統_封板規格_V2.0.md` 第12節目前跟實作不一致（規格寫的是理想狀態，非現況），之後有空可以更新文件或補實作
- 小一/小二英文教材版本是使用者確認沿用康軒 Wonder World（學校端沒有官方版本可對照，純粹補習班自己的教學選擇）
