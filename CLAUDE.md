# ══════════════════════════════════════
# CLAUDE.md — AI 工作守則
# 專案：[mina-web-edu]
# 路徑：/home/manko94134/mina-web-edu/
# 更新：[2026/6/14]
# ══════════════════════════════════════

## 🗣️ 穴居人模式（回應方式）

少字，多資訊。
- 能列表，不寫段落
- 能箭頭，不寫連接詞
- 能表格，不寫文章
- 能直接做，不先解釋
- 刪除客套、重複、廢話

Token ↓　資訊 ↑
少字 ≠ 少資訊，必要內容不可省略。

## 🌐 語言

- 永遠用繁體中文回應（台灣用語）
- 程式碼變數、函數名稱：英文
- 程式碼內的註解：繁體中文
- 保持專案既有程式碼命名風格
- commit message 格式：類型: 繁中說明
  （類型：feat / fix / docs / refactor / test / chore）

## ⚙️ 每次對話開始（自動執行，不需我說）

若存在，開始工作前優先參考：
1. PROJECT_STATE.md
2. docs/architecture.md
3. memory/ 資料夾

用一段話報告：目前做到哪 + 今日建議從哪繼續。

## 🚀 執行規則

- 可自行決定的小事：直接決定，不說「我打算⋯可以嗎？」
- 不要在步驟之間問「是否繼續」，直接執行下一步
- 完成後再報告結果，不要邊做邊解釋過程
- 遇到重大決策：說明問題 → 提供可行方案 → 等待確認
- 只有「刪除檔案」「清除資料庫資料」等不可逆操作，才停下確認

## 🔧 工作規則

- 先閱讀相關檔案，再進行修改
- 只完成被明確要求的範圍，不自行擴大
  （同時要求修5個地方 = 一個任務，全部做完）
- 未被要求時不刪除程式碼
  若需刪除，先說明原因再執行
- 不確定需求就問，不要自行假設
- 出問題先找根本原因，不要直接修或暫時修補
- 優先重用既有實作，避免重複程式碼
- 保持專案架構與風格一致
- 修改前先評估影響範圍
- 完成後自行檢查是否有遺漏

## 🔄 Git 自動化

# 修正完成後一次執行（Claude 自動判斷哪些有改）
git add .
git commit -m "fix: 說明（繁中）"
git push origin dev

# 若有 Workers 變更，額外執行：
cd workers && npx wrangler deploy && cd ..

**不得讓修正停留在未 commit 狀態。**
每次 push 後，Cloudflare Pages 會自動從 dev 分支重新部署前端。
Workers 需手動執行 `npx wrangler deploy`。

規則：
- push 後 Vercel 自動部署，不需額外指令
- .env 永遠不進 Git
- 不要修改 .gitignore
- 不要修改 .env 的實際值
- 不要修改正式環境設定

## 📄 文件自動維護（每完成功能後，自動執行）

1. 更新 PROJECT_STATE.md（完成 / 進行中 / 下一步 / 缺螺絲）
2. 更新 CHANGELOG.md（最新版本在最上面，格式：版本號 / 日期 / 新增 / 修改 / 修正）
3. 技術架構或工具決策 → memory/decisions.md
4. 業務邏輯或用戶規則 → memory/business-rules.md
5. 有新 API endpoint → docs/api.md
6. 修完 Bug → memory/bugs.md（原因 / 解法 / 如何避免）

## 🔒 安全規則

- 不要讀取或輸出 .env 的任何實際值
- 不要在對話中顯示任何密鑰、Token、密碼
- 密鑰用環境變數名稱讀取（process.env.KEY_NAME）
- 不要修改 .gitignore
